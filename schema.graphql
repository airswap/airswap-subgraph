#
# SwapERC20Contract
#

type SwapERC20 @entity(immutable: true) {
  id: Bytes!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: Bytes!
  from: Bytes!
  to: Bytes!
  nonce: BigInt!
  signerWallet: Bytes!
  signerToken: Bytes!
  senderWallet: Bytes!
  senderToken: Bytes!
  signerAmount: BigInt!
  senderAmount: BigInt!
  feeAmount: BigInt!
  feeAmountUSD: BigDecimal!
  senderAmountUSD: BigDecimal!
  signerAmountUSD: BigDecimal!
}

#
# RegistryContract
#

type Server @entity(immutable: true) {
  id: Bytes!
  url: String!
  protocols: [Bytes!]!
  tokens: [Bytes!]!
}

#
# StakingContract
#

type Staker @entity(immutable: true) {
  id: Bytes!
  amount: BigInt!
  initial: BigInt!
}

#
# Aggregates
#

type Daily @entity(immutable: false) {
  id: ID!
  date: Int!
  volume: BigDecimal!
  fees: BigDecimal!
  count: Int!
  max: BigDecimal!
}

type Yearly @entity(immutable: false) {
  id: ID!
  volume: BigDecimal!
  fees: BigDecimal!
  count: Int!
  max: BigDecimal!
}

type Total @entity(immutable: false) {
  id: ID!
  volume: BigDecimal!
  fees: BigDecimal!
  count: Int!
  max: BigDecimal!
}

type TotalStaked @entity(immutable: false) {
  id: ID!
  amount: BigInt!
  count: BigInt!
}
